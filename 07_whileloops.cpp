#include <iostream>

int main() {
    int count = 0;

    while (count < 5) {
        std::cout << "Count is " << count << std::endl;
        count++;  // Increment the counter
    }

    return 0;
}
